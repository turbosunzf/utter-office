# Mobile production env — placeholder for the utter-office backend.
# Loaded by the `*:prod` scripts via dotenv-cli (see package.json).
#
# TODO: point to the real utter-office production backend once available.
EXPO_PUBLIC_API_URL=https://<api-host>
EXPO_PUBLIC_WEB_URL=https://<web-host>
